yt-dlp>=2024.3.10
rich>=13.7.0
keyboard>=0.13.5
pyperclip>=1.8.2

# After installing everything, freeze exact versions
pip freeze > requirements_all.txt

# Or manually create the two files:

# requirements_cli.txt
echo yt-dlp>=2024.3.10 > requirements_cli.txt
echo rich>=13.7.0 >> requirements_cli.txt
echo keyboard>=0.13.5 >> requirements_cli.txt
echo pyperclip>=1.8.2 >> requirements_cli.txt

# Install from file later:
pip install -r requirements_cli.txt
pip install -r requirements_gui.txt

