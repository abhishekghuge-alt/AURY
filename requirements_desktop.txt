# ── AURY Desktop App Dependencies ─────────────────────────────────
# Install with: pip install -r requirements_desktop.txt

# Desktop window (wraps Flask in a native WebView)
pywebview>=4.4.1

# Build standalone .exe (run on Windows only)
pyinstaller>=6.0

# ── Core (same as requirements.txt) ───────────────────────────────
flask>=3.0.0
flask-cors>=4.0.0
yt-dlp>=2024.1.0
ffmpeg-python>=0.2.0
Pillow>=10.0.0
requests>=2.31.0
rich>=13.0.0
schedule>=1.2.0
psutil>=5.9.0
pyperclip>=1.8.0
python-dotenv>=1.0.0
